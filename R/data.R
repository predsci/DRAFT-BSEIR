# Roxygen descriptions of files in data/


#' Sample input data.frame for DRAFT::runbSEIR()
#' 
#' Incidence data for San Diego County confirmed influenza cases from the 2017-2018 season.
#' 
#' @docType data
#' 
#' @usage data(incidence_data1)
#' 
#' @format A data.frame with columns \code{"date"} and \code{"cases"}.  The \code{"date"} column is of class "Date" as expected by \code{runbSEIR()}.
#' 
#' 
"incidence_data1"



#' Sample input data.frame for DRAFT::runbSEIR()
#' 
#' Incidence data for Liberia ebola cases from the 2013 outbreak.
#' 
#' @docType data
#' 
#' @usage data(incidence_data3)
#' 
#' @format A data.frame with columns \code{"date"} and \code{"cases"}.  The \code{"date"} column is of class "Date" as expected by \code{runbSEIR()}.
#' 
#' 
"incidence_data3"



#' Sample input data.frame for DRAFT::runbSEIR()
#' 
#' Incidence data for San Diego County confirmed influenza cases from the 2014-2015 season.  Trailing rows of 'cases' column are NA indicating that a forecast is requested from \code{DRAFT}.
#' 
#' @docType data
#' 
#' @usage data(incidence_data2)
#' 
#' @format A data.frame with columns \code{"date"} and \code{"cases"}.  The \code{"date"} column is of class "Date" as expected by \code{runbSEIR()}.
#' 
#' 
"incidence_data2"



#' Sample input data.frame for DRAFT::runbSEIR()
#' 
#' Incidence data for Liberia ebola cases from the 2013 outbreak.  Trailing rows of 'cases' column are NA indicating that a forecast is requested from \code{DRAFT}.
#' 
#' @docType data
#' 
#' @usage data(incidence_data4)
#' 
#' @format A data.frame with columns \code{"date"} and \code{"cases"}.  The \code{"date"} column is of class "Date" as expected by \code{runbSEIR()}.
#' 
#' 
"incidence_data4"

